kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: "{{ .Values.appName }}-mongodb-sc"
  namespace: "{{ .Values.appName }}-ns"
  labels:
    app: "{{ .Values.appName }}"
provisioner: kubernetes.io/no-provisioner
#volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true