apiVersion: v1
kind: Pod
metadata:
  name: source-pod
  namespace: "{{ .Values.appName }}-ns"
spec:
  containers:
    - name: busybox
      image: yauritux/busybox-curl
      command: ["/bin/sh", "-c", "sleep 3600"]
      resources:
        requests:
          memory: 1Gi
          cpu: 0.5